# Exact versions of the pinned validation environment (see manifests/environment_manifest.txt).
# Interpreter and system library (NOT pip-installable) — recorded for reproducibility:
#   python==3.13.14
#   libopenslide==4.0.1   (install via conda-forge `openslide` or an OS package)
#
# torch is recorded as the CPU build actually used; swap for a CUDA build on GPU.
torch==2.12.1
segmentation-models-pytorch==0.3.1
numpy==2.4.2
pillow==12.2.0
opencv-python==4.13.0
openslide-python==1.4.6
openslide-bin
wsidicom==0.31.0
pydicom==3.0.2
idc-index
imagecodecs
tifffile
matplotlib
pandas
pyyaml
requests
pytest
